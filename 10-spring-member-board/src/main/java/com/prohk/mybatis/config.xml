<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<!-- mybatis를 사용하는 이유는 sql 개발 분리 -->
<configuration>
	<!-- <properties resource="com/prohk/mybatis/db.properties" /> -->
	<typeAliases> <!-- 별명짓기 -->
		<typeAlias type="com.prohk.model.MemberDto" alias="MemberDto"/>
		<typeAlias type="com.prohk.model.ReplyBoardDto" alias="ReplyBoardDto"/>
	</typeAliases>
	<!-- <environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${driver}" />
				<property name="url" value="${url}" />
				<property name="username" value="${username}" />
				<property name="password" value="${password}" />
			</dataSource>
		</environment>
	</environments>
	<mappers>
		<mapper resource="com/prohk/mybatis/MemberMapper.xml" />
		<mapper resource="com/prohk/mybatis/BoardMapper.xml" />
	</mappers> -->
</configuration>